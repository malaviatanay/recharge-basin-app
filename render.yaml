services:
  - type: web
    name: recharge-basin-app
    runtime: node
    buildCommand: npm install
    startCommand: node src/server/server.js
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: PORT
        sync: false
